import 'package:flutter/material.dart';

class EarningsAllScreen extends StatelessWidget {
  const EarningsAllScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}